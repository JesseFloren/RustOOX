class Foo {
    int x;

    static void f(Foo foo) {
        foo.x := 5;
        fork foo.incr();
        int x := foo.x;
        assert(x == 5);
    }

    void incr() {
        int y := this.x;
        y := y + 0;
        this.x := y;
    }
}